apiVersion: v1
kind: ConfigMap
metadata:
  name: mower-input-data
  namespace: {{ .Values.namespace }}
  labels:
    app: {{ .Values.app.name }}
data:
  instructions.txt: |
    5 5
    1 2 N
    GAGAGAGAA
    3 3 E
    AADAADADDA
  application.properties: |
    environment={{ .Values.environment }}
    job.id={{ .Values.pipelineId }}